name: Test

on: push

jobs:
  main:
    runs-on: self-hosted
    
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run
        run: |
          /Users/eugene/Work/run-agent/run-agent gh-debian-11 10 <<'EOF'
          ls
          pwd
          uname
          EOF


